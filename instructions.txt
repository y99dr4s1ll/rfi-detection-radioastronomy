conda create -n rfi-detection python=3.10
conda activate rfi-detection
conda install numpy=1.24.3 
pip install tensorflow==2.15.0
conda install -c pytorch faiss-gpu
pip install -r requirements.txt
pip install -e ".[dev]"
cd C:\Users\francesco\Documents\TESI\DOMANDA\pres11\New folder
git clone https://github.com/mesarcik/RFI-NLN
python -c "import site; print(site.getsitepackages()[0])"

git add .
git commit -m "descrizione delle modifiche"
git push


# TO-DO
+ ottenere stessi risultati cusum luserna
- runnare sum_threshold luserna
- inserire funzionamento lofar
- inserire feature extraction
- inserire metodi simple, knn, rf, unet, rnet
- inserire gridsearch
- valurare se valori di clipping dinamici da percentili o altro, per ora statici
